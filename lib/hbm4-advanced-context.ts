import type { Localized } from './challenges';
import type { LabContext } from './architectures';

const b = (zh: string, en: string): Localized => ({ zh, en });
const ctx = (blockId: string, placementZh: string, placementEn: string, whyZh: [string, string], whyEn: [string, string], reviewZh: [string, string, string], reviewEn: [string, string, string], boundaryZh: string, boundaryEn: string): LabContext => ({
  blockId,
  placement: b(placementZh, placementEn),
  why: [b(whyZh[0], whyEn[0]), b(whyZh[1], whyEn[1])],
  review: [b(reviewZh[0], reviewEn[0]), b(reviewZh[1], reviewEn[1]), b(reviewZh[2], reviewEn[2])],
  boundary: b(boundaryZh, boundaryEn),
});

export const hbm4AdvancedContext: Record<string, LabContext> = {
  'hbm-dual-command-gate': ctx('adapter', '位於每個 channel scheduler 與 HBM PHY adapter 之間，將 row/column winners 轉成一次原子 command bundle。', 'Between each channel scheduler and the HBM PHY adapter, converting row/column winners into one atomic command bundle.', ['HBM4 的 row/column 介面可以提高命令頻寬，但 common command 仍會跨兩個 PC。', '只做兩個獨立 valid 而沒有 bundle commit，PHY backpressure 時會造成 metadata 與 pin-level command 不一致。'], ['Dual row/column interfaces increase command bandwidth, but common commands still span both PCs.', 'Two independent valids without an atomic bundle commit desynchronize metadata under PHY backpressure.'], ['哪些 row/column 組合可以同拍？判斷依據放在哪一層？', '為何 row_fire/col_fire 而不是 candidate_valid 更新 timing state？', '如果 PHY 每兩拍才 ready，如何保持 candidate snapshot？'], ['Which row/column pairs may share a cycle, and where is that legality decided?', 'Why must row_fire/col_fire—not candidate_valid—update timing state?', 'How is the candidate snapshot retained when the PHY is ready every other cycle?'], 'RTL 只輸出數位 command bundle；CK edge alignment、setup/hold 與 microbump electrical behavior由 PHY/physical design 負責。', 'RTL emits a digital command bundle; CK-edge alignment, setup/hold, and microbump electrical behavior belong to PHY/physical design.'),
  'hbm-bank-state-table': ctx('bank', '每個 pseudo-channel 一組分散式 bank metadata，供 command generator、row-hit policy 與 timing mask 同時查詢。', 'Distributed bank metadata per pseudo-channel, queried by the command generator, row-hit policy, and timing masks.', ['Bank state 是所有 ACT/PRE/RD/WR legality 的共同真相來源。', '1024-bank 系統若複製寬組合路徑，會形成面積與扇出問題；需分片或 RAM-backed。'], ['Bank state is the source of truth for ACT/PRE/RD/WR legality.', 'A 1024-bank design needs sharding or RAM-backed metadata to avoid area and fanout problems.'], ['同拍 PRE 與下一個 ACT 如何定義 bypass？', 'Metadata 在 PHY 不接受 command 時是否可先更新？', 'Power-down/self-refresh 回來後 open-row state 如何收斂？'], ['How is same-cycle PRE-to-next-ACT bypass defined?', 'May metadata update before the PHY accepts the command?', 'How does open-row state converge after power-down/self-refresh?'], 'DRAM array 的真正開列狀態不可直接觀察；controller 必須以 accepted-command history 維持一致的 architectural mirror。', 'The array’s true open-row state is not directly observable; the controller maintains an architectural mirror from accepted-command history.'),
  'hbm-row-timing-scoreboard': ctx('timing', '位於 bank state 與 scheduler candidate mask 之間，每 bank 或每 timing scope 產生 act_ok/col_ok/pre_ok。', 'Between bank state and scheduler candidate masking, producing act_ok/col_ok/pre_ok per bank or timing scope.', ['tRCD、tRAS、tRP 同時活動，不能用單一 busy 取代。', 'Counter 必須以真正 issue 啟動，且產品值需正確處理 ns→controller-cycle rounding。'], ['tRCD, tRAS, and tRP overlap and cannot be replaced by one busy bit.', 'Counters start on true issue, and product values require correct ns-to-controller-cycle rounding.'], ['Counter、absolute deadline、timing wheel 的 PPA 取捨？', 'DVFS 時未到期 timer 如何轉換？', '如何用 assertion 證明早一拍永遠不會放行？'], ['PPA tradeoffs among counters, absolute deadlines, and timing wheels?', 'How are active timers converted across DVFS?', 'How does an assertion prove the block never releases one cycle early?'], '規格提供 AC minima；PHY/clock ratio 合約決定 controller 看見的離散週期數。', 'The standard supplies AC minima; the PHY/clock-ratio contract determines discrete controller cycles.'),
  'hbm-activate-window': ctx('timing', '位於每個 pseudo-channel 的 ACT candidate mask；bank-local legality、BG spacing 與 rolling power window 在這裡匯合。', 'In each pseudo-channel ACT candidate mask, combining bank-local legality, BG spacing, and the rolling power window.', ['tRRD 與 tFAW 是不同 scope 的 constraint，候選必須同時通過。', 'Rolling-window corner 比單一倒數器更容易出現第 5 個 ACT off-by-one。'], ['tRRD and tFAW are different-scope constraints, and a candidate must pass both.', 'Rolling-window corners make the fifth-ACT off-by-one more likely than a simple timer.'], ['第 4 與第 5 個 ACT 的 window 邊界怎麼畫？', '若 blocked candidate 也寫 history，會造成什麼效能/正確性問題？', '1024 banks 要如何共享 PC-level tFAW state？'], ['Draw the exact boundary for the fourth and fifth ACT.', 'What fails if a blocked candidate writes history?', 'How do 1024 banks share PC-level tFAW state?'], 'tFAW 反映電流/供電限制但 RTL 只執行離散命令窗；power integrity 由封裝與實體驗證。', 'tFAW reflects current/power limits, while RTL enforces a discrete command window; package and physical flows verify power integrity.'),
  'hbm-rw-turnaround': ctx('timing', '在 column scheduler 前產生 direction mask，與 tCCD、bank state、refresh mask 一起形成 eligible。', 'Produces a direction mask before the column scheduler, combined with tCCD, bank state, and refresh masks to form eligible.', ['只檢查 tCCD 仍可能在讀寫換向時碰撞 data bus。', 'W→R 可能依 bank-group 關係使用不同限制，不能用一個固定 turnaround。'], ['Checking only tCCD can still collide on the data bus during direction changes.', 'W→R may depend on bank-group relation and cannot use one fixed turnaround.'], ['為何 same-direction 要交給 tCCD 而不是重複限制？', 'Write-drain policy 如何使用這個 guard 減少 bubble？', 'RL/WL 改變時哪些 turnaround parameter 需要重算？'], ['Why should same-direction spacing stay in tCCD instead of being duplicated?', 'How does write-drain policy use this guard to reduce bubbles?', 'Which turnaround parameters change when RL/WL changes?'], 'WDQS、DQ tri-state、read capture 都在 PHY；controller 負責不在數位排程上造成方向重疊。', 'WDQS, DQ tristate, and read capture live in PHY; the controller prevents direction overlap in digital scheduling.'),
  'hbm-refresh-credit': ctx('refresh', 'Refresh deadline manager 的 accounting kernel，輸出 normal/urgent priority 給每 PC maintenance arbiter。', 'Accounting kernel in the refresh deadline manager, driving normal/urgent priority into each PC maintenance arbiter.', ['只設固定週期 pulse 無法表示 postpone/pull-in，也無法檢查最壞 deadline。', 'Debt/credit 讓 traffic policy 與 retention correctness 分層。'], ['A fixed periodic pulse cannot model postpone/pull-in or check worst-case deadlines.', 'Debt/credit separates traffic policy from retention correctness.'], ['同拍 interval 到期與 refresh issued 為何要 net-zero？', 'All-bank 與 per-bank refresh 是否共用同一 debt？', '溫度切換 tREFI 時如何避免既有 debt 被清空？'], ['Why should a same-cycle interval and refresh net to zero?', 'Do all-bank and per-bank refresh share debt?', 'How is existing debt preserved when temperature changes tREFI?'], 'Retention physics 與 temperature sensor 在 DRAM/PHY；controller 消費 profile/status 並保證命令 deadline。', 'Retention physics and temperature sensing are in DRAM/PHY; the controller consumes status/profile and guarantees command deadlines.'),
  'hbm-rfm-counter': ctx('refresh', '與一般 refresh manager 並列的 row-hammer mitigation metadata；每次 accepted ACT 更新 bank-local RAA。', 'Row-hammer mitigation metadata beside the normal refresh manager, updating bank-local RAA on each accepted ACT.', ['RFM 是普通 retention refresh 之外的獨立資料完整性義務。', 'RAA metadata 若 scope 或 decrement 寫錯，可能漏發 RFM 或過度犧牲頻寬。'], ['RFM is a data-integrity obligation separate from ordinary retention refresh.', 'Wrong RAA scope or decrement can miss RFM or waste bandwidth.'], ['RAA threshold/decrement 從哪裡取得？', 'ACT 與 RFM 同拍時 priority 為何必須規格化？', '1024 counters 如何放入 RAM 又維持一拍 update？'], ['Where do RAA thresholds/decrements come from?', 'Why must same-cycle ACT/RFM priority be specified?', 'How can 1024 counters live in RAM while sustaining one update per cycle?'], '鄰近列物理關係與 vendor 內部 refresh 不可見；controller 只管理規格提供的抽象 counters/commands。', 'Physical row adjacency and vendor-internal refresh are invisible; the controller manages only the specified abstract counters and commands.'),
  'hbm-drfm-sequencer': ctx('refresh', '位於 RAS/maintenance sequencer，跨越 bank state、command generator 與 RFM issue port，保存一筆 DRFM transaction。', 'In the RAS/maintenance sequencer across bank state, command generation, and the RFM issue port, owning one DRFM transaction.', ['DRFM 是多拍 protocol，不是單一 pulse；captured address 必須有明確 ownership。', '第二個 sample 若覆寫第一個，可能對錯誤 row 做維護且難以在 post-silicon 追查。'], ['DRFM is a multi-cycle protocol, not one pulse; the captured address needs explicit ownership.', 'Overwriting a first sample with a second may service the wrong row and is hard to debug post-silicon.'], ['為何 captured row 必須保留到 DRFMpb accepted？', '一般 request 與 DRFM PRE 競爭同 bank 時誰優先？', '多 outstanding DRFM 要用 FIFO 還是每 bank slot？'], ['Why retain the captured row until DRFMpb is accepted?', 'Who wins when normal traffic and DRFM PRE target one bank?', 'Should multiple outstanding DRFM transactions use a FIFO or per-bank slots?'], 'DRAM 如何刷新實體鄰近列屬於 die 內部；controller 僅提供捕捉與 service 命令序列。', 'How DRAM refreshes physical neighbors is die-internal; the controller provides only capture and service sequencing.'),
  'hbm-mrs-quiesce': ctx('adapter', '跨 frontend admission、兩個 PC queues、command adapter 的 shared-configuration transaction manager。', 'Shared-configuration transaction manager spanning frontend admission, both PC queues, and the command adapter.', ['Mode register 由兩個 PC 共用，不能只讓其中一邊 idle 就更新。', 'MRS 後立即恢復 traffic 會違反 tMOD 或使用到新舊參數混合狀態。'], ['Mode registers are shared by both PCs; idling only one is insufficient.', 'Immediate traffic after MRS violates tMOD or mixes old/new configuration state.'], ['pc_idle 必須包含哪些 outstanding 狀態？', 'MRS 被 PHY backpressure 時 tMOD 從哪一拍開始？', 'Firmware timeout 後要 rollback 還是 reset？'], ['What outstanding state must pc_idle include?', 'When PHY backpressures MRS, which cycle starts tMOD?', 'After firmware timeout, should the controller roll back or reset?'], 'MR 內容與類比 training effect 由 DRAM/PHY 定義；controller 負責 quiesce、commit、wait 的數位原子性。', 'DRAM/PHY define MR contents and analog training effects; the controller owns digital quiesce/commit/wait atomicity.'),
  'hbm-ca-parity': ctx('adapter', '位於 command encoder 的最後數位 stage，緊鄰 PHY；AERR 經同步後進入 RAS/CSR/IRQ。', 'At the last digital stage of the command encoder next to PHY; synchronized AERR feeds RAS/CSR/IRQ.', ['CA parity 可偵測傳輸錯誤，但 DRAM 仍可能執行命令，因此不能只丟掉 error pulse。', 'Sticky fault 與 command history 是 field debug 的必要證據。'], ['CA parity detects transfer errors, but DRAM may still execute the command, so the error pulse cannot be discarded.', 'Sticky fault state and command history are essential for field debug.'], ['Parity bundle 為何要依 CK edge 分開定義？', 'AERR 是 pulse、level 還是 asynchronous signal，怎麼同步？', '偵測 error 後 recovery policy 為何不應硬塞進 parity generator？'], ['Why define the parity bundle per CK edge?', 'Is AERR a pulse, level, or asynchronous signal, and how is it synchronized?', 'Why should recovery policy remain outside the parity generator?'], 'Pin-level APAR/AERR timing、metastability hardening 與 IO voltage 由 PHY/CDC/physical signoff；本題從同步數位界面開始。', 'Pin-level APAR/AERR timing, metastability hardening, and IO voltage belong to PHY/CDC/physical signoff; this lab starts at a synchronous digital interface.'),
  'hbm-power-state': ctx('adapter', 'Channel-common power sequencer 橫跨 admission、兩個 PC drain、command adapter 與 PHY status。', 'Channel-common power sequencer spanning admission, dual-PC drain, command adapter, and PHY status.', ['Power-down/self-refresh 是兩個 PC 共用狀態；只排空一邊會遺失或凍結另一邊 transaction。', 'PDE/PDX 是 protocol commit，不能用內部 sleeping flag 先行猜測。'], ['Power-down/self-refresh are shared by both PCs; draining only one loses or freezes the other side.', 'PDE/PDX are protocol commits, so an internal sleeping flag cannot predict them early.'], ['新 request 與 sleep_req 同拍時 admission 語意？', '如何證明沒有 PDE/PDX 同拍？', 'Wake timeout、reset 與 refresh debt 怎麼交互？'], ['What is admission behavior for same-cycle new request and sleep_req?', 'How is mutual exclusion of PDE and PDX proven?', 'How do wake timeout, reset, and refresh debt interact?'], 'Clock gating cells、rail isolation 與電源 ramp 是 UPF/physical/PHY 工作；controller 管理 command-level quiescence。', 'Clock-gating cells, rail isolation, and power ramps belong to UPF/physical/PHY; the controller manages command-level quiescence.'),
  'hbm-stack-dispatch': ctx('channel', '在 NoC/AXI frontend 後的第一級 pipeline，把請求切到 32 個 channel，再進各自兩個 PC queues。', 'First pipeline stage after the NoC/AXI frontend, steering requests into 32 channels and then two PC queues per channel.', ['32 channel 若用一個扁平 scheduler，critical path、扇出與 floorplan 都不可控。', 'ready/valid snapshot 不原子會把 request 路由到地址改變後的錯誤 channel。'], ['One flat scheduler across 32 channels creates uncontrolled critical paths, fanout, and floorplanning.', 'A non-atomic ready/valid snapshot can route a request using a later address.'], ['為什麼 channel ready 可以組合讀但 route payload 要 registered？', 'Hash/interleave 如何在不改 software-visible layout 下演進？', '32 個 channel 到 PHY slices 的 pipeline 切點由誰決定？'], ['Why may channel ready be read combinationally while route payload is registered?', 'How can hashing/interleaving evolve without changing software-visible layout?', 'Who decides pipeline cuts between 32 channels and PHY slices?'], 'Microbump topology與 channel placement 是 package/physical input；RTL 只實作經核准的 address-map contract。', 'Microbump topology and channel placement are package/physical inputs; RTL implements only the approved address-map contract.'),
};
